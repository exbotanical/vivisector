sudo: false
language: node_js
node_js:
    - "13.6"
branches:
    only:
        - master
cache:
    directories:
        - node_modules
before_install:
    - npm update
    - sudo apt-get update
install:
    - npm install
script:
    # - npm test
    - npm run coveralls